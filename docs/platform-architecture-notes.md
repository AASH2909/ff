# Platform Architecture Notes

## PWA Release State

`public/sw.js` is committed intentionally as part of the current PWA release state. Build runs may regenerate it when the route manifest or precache list changes.
